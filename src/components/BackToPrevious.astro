---
type Props = {
  href: string;
};

import { Icon } from "astro-icon/components";

const { href } = Astro.props;
---

<a href={href} class="sphere-back-link not-prose">
  <Icon name="tabler:arrow-left" size={16} aria-hidden="true" />
  <span><slot /></span>
</a>
