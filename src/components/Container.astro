---
type Props = {
  wide?: boolean;
};

const { wide = false } = Astro.props;
---

<div
  class:list={[
    "mx-auto w-full px-5 sm:px-7",
    wide ? "max-w-[68rem]" : "max-w-[48rem]",
  ]}
>
  <slot />
</div>
